/**
 * @repo/database/test
 *
 * Test utilities for database testing.
 * Only import this in test files!
 *
 * @example
 * ```ts
 * // In test files only:
 * import {
 *   getTestClient,
 *   createTestUser,
 *   cleanDatabase
 * } from "@repo/database/test";
 * ```
 */

// Export test utilities
export * from '../test/utils';
export * from '../test/fixtures';

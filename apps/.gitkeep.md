# Apps Directory

This directory will contain the Next.js applications:

- `admin/` - Admin panel application
- `web/` - User-facing application
- `workers/` - Background job workers

These will be created in subsequent tasks.

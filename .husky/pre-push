#!/bin/sh

# Pre-push hook
# Run type-checks before pushing to remote

echo "🔍 Running pre-push typecheck..."

pnpm type-check
if [ $? -ne 0 ]; then
  echo "❌ Typecheck failed. Please fix type errors before pushing."
  exit 1
fi

echo "✅ Pre-push typecheck passed!"
